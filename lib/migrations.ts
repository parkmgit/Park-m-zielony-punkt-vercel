// PostgreSQL migrations
// Schema is defined in lib/db.ts initDB()

export function runMigrations() {
  // Migrations are handled in lib/db.ts initDB()
  console.log('✓ Migrations handled by PostgreSQL schema');
}
